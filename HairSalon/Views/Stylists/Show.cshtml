@{
  Layout = "_Layout";
}
@using HairSalon.Models;
<nav>
  <a href='/stylists/@Model.GetId()/clients/new'>Add new client</a>
</nav>

<h3>@Model.GetFirstName() @Model.GetLastName() @Model.GetPhoneNumber()</h3>

@if (@Model.GetClients(@Model.GetId()).Count == 0)
{
  <p>Currently, this stylist does not have any clients</p>
}
@if (@Model.GetClients(@Model.GetId()).Count != 0)
{
  <table>
    <tr>
      <th>Name</th>
      <th>Phone</th>
    </tr>
 
      @foreach ( Client client in @Model.GetClients(Model.GetId()))
      {
        <tr>
          <td><a href='/stylists/@Model.GetId()/clients/@client.GetId()'>@client.GetFirstName() @client.GetLastName()</a></td>
          <td>@client.GetPhoneNumber()</td>
        </tr>
      }
 </table>

}